apply from: "${project.rootDir}/config/common/module.gradle"
apply plugin: "java-library"
apply from: "${project.rootDir}/config/common/publish.gradle"

version =  scmVersion.version

publishing {
    publications {
        maven(MavenPublication) {
            artifactId = "common-$project.name"
        }
    }
}